"use client";

// Career Launch 수료증 라우트 — 완주 학생에게 발급(인쇄/PDF).
import { CareerLaunchCertificate } from "../../../components/launch/CareerLaunchCertificate";

export default function CareerLaunchCertificateRoute() {
  return <CareerLaunchCertificate />;
}
