import { SectionView } from "../../_components/SectionView";

export default function AdminUsersPage() {
  return (
    <SectionView
      title="전체 사용자 관리"
      description="운영자 계정 생성, 권한 설정, 접속 제어 정책을 관리합니다."
    />
  );
}
