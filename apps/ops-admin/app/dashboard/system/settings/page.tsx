import { SectionView } from "../../_components/SectionView";

export default function SettingsPage() {
  return (
    <SectionView
      title="설정값 관리"
      description="시스템 기본값, 운영 정책, 환경 설정값을 관리합니다."
    />
  );
}
